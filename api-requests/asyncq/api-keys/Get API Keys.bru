meta {
  name: Get API Keys
  type: http
  seq: 2
}

get {
  url: {{host}}/v1/api-keys
  body: json
  auth: inherit
}

script:pre-request {
  req.setHeader("Authorization", "Bearer " + bru.getEnvVar("token"))
}
